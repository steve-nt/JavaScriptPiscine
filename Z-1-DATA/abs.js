function isPositive(num) {
  return num > 0
}

function abs(num) {
  return num < 0 ? -num : num
}

/* 
 * 📚 WHAT YOU LEARNED:
 * - Ternary operators (condition ? true : false) for concise conditional logic
 * - Comparison operators (>, <) to evaluate conditions
 * - Implementing mathematical concepts (absolute value) without built-in functions
 * - Boolean return values from functions
 * 
 * 🎯 PRACTICE MORE:
 * - Create a function to check if a number is even/odd
 * - Build a calculator with custom math operations
 * - Write a function that returns the largest of three numbers
 * 
 * 🌍 REAL-WORLD EXAMPLES:
 * - E-commerce: Calculating price differences (always positive)
 * - GPS apps: Finding distance between locations (always absolute)
 * - Banking apps: Showing transaction amounts without negative signs
 * - Game development: Calculating damage amounts or health changes
 * 
 * 💡 Keep practicing! Understanding conditionals is fundamental to programming! 
 */
