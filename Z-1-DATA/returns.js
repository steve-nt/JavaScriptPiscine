function id(value) {
  return value
}

function getLength(arr) {
  return arr.length
}

/* 
 * 📚 WHAT YOU LEARNED:
 * - Identity function: Returns input unchanged
 * - Function return values
 * - Accessing properties with dot notation (.length)
 * - Arrays and strings both have .length property
 * - Functions as simple transformations
 * 
 * 🎯 PRACTICE MORE:
 * - Create a function that returns the type of a value
 * - Build a function that returns the first character
 * - Make a function that checks if an array is empty
 * - Write a function that returns array statistics (length, first, last)
 * 
 * 🌍 REAL-WORLD EXAMPLES:
 * - Identity function: Used in functional programming, mapping, filtering
 * - Password validation: Checking minimum length requirements
 * - Character counters: Twitter's 280 character limit
 * - File uploads: Checking if files were selected (length > 0)
 * - Pagination: Calculating total pages from array length
 * 
 * 💡 Even simple functions serve important purposes in larger systems! 
 */
