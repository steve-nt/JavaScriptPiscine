function concatStr(str1, str2) {
  return String(str1) + String(str2)
}

/* 
 * 📚 WHAT YOU LEARNED:
 * - String concatenation using the + operator
 * - Type conversion with String() constructor
 * - Handling different input types (numbers, strings, etc.)
 * - Explicit type coercion vs implicit coercion
 * 
 * 🎯 PRACTICE MORE:
 * - Create a function that joins multiple strings with a separator
 * - Build a template string function (like `Hello ${name}!`)
 * - Make a function that concatenates arrays of strings
 * - Write a URL builder that combines base URL and query parameters
 * 
 * 🌍 REAL-WORLD EXAMPLES:
 * - Building full names: firstName + lastName
 * - Creating email addresses: username + '@' + domain
 * - Generating file paths: folder + '/' + filename
 * - Building SQL queries: combining query parts safely
 * - Creating display text: price + ' ' + currency
 * 
 * 💡 String manipulation is one of the most common tasks in programming! 
 */
